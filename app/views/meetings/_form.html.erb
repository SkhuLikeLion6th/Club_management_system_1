
<%= form_for(meeting) do |f| %>
  <% if meeting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(meeting.errors.count, "error") %> prohibited this meeting from being saved:</h2>

      <ul>
      <% meeting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :제목 %>
    <%= f.text_field :name %>
  </div>

  <div class="field mt60">
    <%= f.label :내용 %>
    <%= f.text_area :content %>
  </div>
  



  <div class="field">
    <%= f.label :시작시간 %>
    <div class="ml40"><%= f.datetime_select :start_time , {:class => "selc"}%></div>
  </div>

  <div class="field ">
    <%= f.label :끝나는시간 %>
    <div class="ml40"><%= f.datetime_select :end_time %></div>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
